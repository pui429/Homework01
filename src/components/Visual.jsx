import VisualContent from "./VisualContent";
import "./Visual.scss";

const Visual = () => {
  return (
    <section id="visual">
      <VisualContent />
    </section>
  );
};

export default Visual;
